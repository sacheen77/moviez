apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moviez.fullname" . }}-test-connection"
  labels:
    {{- include "moviez.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moviez.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
